version: "2"

services:
  php:
    image: wodby/drupal:$DRUPAL_TAG
    environment:
      PHP_FPM_CLEAR_ENV: "no"
    volumes:
      - ./:/var/www/html:cached

  nginx:
    volumes:
      - ./:/var/www/html:cached
