ps267.dashboard:
  path: '/admin/dashboard'
  defaults:
    _controller: '\Drupal\ps267\Controller\PS267Controller::adminDashboard'
    link_id: 'ps267.admin_content'
    _title: 'Dashboard'
  requirements:
    # No one should be using this (for now; if we improve it in the future then we can restore access).
    _access: 'FALSE'
